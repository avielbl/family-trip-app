rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Photos are under trips/{tripCode}/photos/{photoId}
    match /trips/{tripCode}/photos/{photoId} {
      allow read, write: if request.auth != null;
    }
  }
}
